version: '2'

services:
  joki:
    build: .
    working_dir: /build/recipes/joki
    environment:
      - "GOPATH=/build/recipes/joki"
      - "GOBIN=${GOPATH}/bin"
    command: fpm-cook
    volumes:
      - .:/build
      - ./pkg:/pkg
